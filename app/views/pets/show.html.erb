<div class="banner d-flex justify-content-center">
  <div >
    Welcome
  </div>
</div>

<%= link_to '<- retour', pets_path %>

<h1><%= @pet.name %> by <%= @pet.owner %></h1>

<h2><%= @pet.species %></h2>

<h3><%= @pet.age %></h3>

<%= link_to 'edit', edit_pet_path(@pet) %>
<%= link_to 'delete', pet_path(@pet), method: :delete %>
